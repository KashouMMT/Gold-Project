<!-- Pagination START -->
<nav class="d-flex justify-content-center mt-5" aria-label="navigation">
    <ul class="pagination pagination-primary-soft rounded mb-0">
        <li class="page-item mb-0" th:classappend="${currentPage == 0} ? 'disabled'">
            <a class="page-link" th:href="@{/blogs(page=${currentPage - 1})}" tabindex="-1">
                <i class="fas fa-angle-double-left"></i>
            </a>
        </li>
        <li class="page-item mb-0" th:each="i : ${#numbers.sequence(0, totalPages - 1)}"
            th:classappend="${i == currentPage} ? 'active'">
            <a class="page-link" th:href="@{/blogs(page=${i})}" th:text="${i + 1}"></a>
        </li>
        <li class="page-item mb-0" th:classappend="${currentPage == totalPages - 1} ? 'disabled'">
            <a class="page-link" th:href="@{/blogs(page=${currentPage + 1})}">
                <i class="fas fa-angle-double-right"></i>
            </a>
        </li>
    </ul>
</nav>
<!-- Pagination END -->
